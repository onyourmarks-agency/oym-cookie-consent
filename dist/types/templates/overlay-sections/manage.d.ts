import type { ConfigType } from '../../_types/config';
import type { ContentManageType } from '../../_types/content';
export declare const renderTemplateManage: (content: ContentManageType, config: ConfigType) => string;
