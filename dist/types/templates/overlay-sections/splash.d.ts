import type { ConfigType } from '../../_types/config';
import type { ContentSplashType } from '../../_types/content';
export declare const renderTemplateSplash: (content: ContentSplashType, config: ConfigType) => string;
