import type { ContentType } from '@js/_types/content';

import nl from './nl';
import en from './en';

const translation: {
  nl: ContentType;
  en: ContentType;
} = {
  nl,
  en,
};

export default translation;
