import type { ContentType } from '@tdecc/_types/content';

import nl from './nl';
import en from './en';

const translation: {
  nl: ContentType;
  en: ContentType;
} = {
  nl,
  en,
};

export default translation;
